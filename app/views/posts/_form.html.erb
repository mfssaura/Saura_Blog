<!-- My custom form
<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href="posts/_form_style.css" type="text/css">
</head>
<body>
<div class="form-section">
  <%= simple_form_for @post do |f| %>
    <div class="field">
      <div class="control">
        <%= f.input :title, input_html: { class: 'input' }, wrapper: false, label_html: { class: 'label'} %>
      </div>
    </div>

    <div class="field">
      <div class="control">
        <%= f.input :content, :as => :ckeditor, input_html: {:ckeditor => {:toolbar => 'FULL'}}, class: "input" %>
      </div>
    </div>
    <%= f.button :submit, 'Create post', class: "button-is-primary" %>
  <% end %>
</div>
</body>
</html>
-->

<!-- Bootstrap form -->
<%= simple_form_for @post do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= "#{pluralize(@post.errors.count, "error")} prohibited this post from being saved:" %>
      </h2>
      <ul>
        <% @post.errors.full_messages.each do |msg| %>
          <li>
            <%= msg %>
          </li>
          <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.input :title, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.input :content, :as => :ckeditor, input_html: {:ckeditor => {:toolbar => 'FULL'}}, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.button :submit %>
  </div>
  
<% end %>
